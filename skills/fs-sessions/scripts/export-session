#!/usr/bin/env python3
"""SessionEnd hook — reads JSON from stdin, exports transcript to shared repo."""

import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent.parent))

from fs_sessions.cli import hook_main  # noqa: E402

hook_main()
