#!/usr/bin/env python3
"""Legacy SessionEnd entry point; delegate to the policy-controlled hook."""

import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent.parent))

from fs_sessions.cli import main  # noqa: E402

raise SystemExit(main(["hook", "run"]))
